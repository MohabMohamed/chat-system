version: "3.7"
services:
  rails_chat_dev:
      build:
        context: ./rails-chat-service
        target: development
      env_file:
        - ./rails-chat-service/config/envfiles/development.env
      ports:
        - 3000:3000
      depends_on:
        - db
      restart: always
      links:
        - db    
      volumes: 
        - "./rails-chat-service:/app"
        - /app/tmp/
  go_chat_dev:
      build:
        context: ./go-chat-service
        target: development
      env_file:
        - ./go-chat-service/config/development.env
      ports:
        - 8000:8000
      depends_on:
        - db
      restart: always
      links:
        - db    
      volumes: 
        - "./go-chat-service:/app"
  db:
    image: mysql:5.7
    restart: always
    env_file:
      - ./rails-chat-service/config/envfiles/development.env
    ports: 
      - '3307:3306'
    volumes:
      - ~/mysql_data/chat_system_dev:/var/lib/mysql
  es01:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.5.2
    container_name: es01
    environment:
      - node.name=es01
      - cluster.name=es-docker-cluster
      - discovery.seed_hosts=es02
      - cluster.initial_master_nodes=es01,es02
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - data01_dev:/usr/share/elasticsearch/data
    ports:
      - 9200:9200
    networks:
      - elastic
  es02:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.5.2
    container_name: es02
    environment:
      - node.name=es02
      - cluster.name=es-docker-cluster
      - discovery.seed_hosts=es01
      - cluster.initial_master_nodes=es01,es02
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - data02_dev:/usr/share/elasticsearch/data
    ports:
      - 9200:9200      
    networks:
      - elastic
volumes:
  data01_dev:
    driver: local
  data02_dev:
    driver: local
networks:
  elastic:
    driver: bridge  